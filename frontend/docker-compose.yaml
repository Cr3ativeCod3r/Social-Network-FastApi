version: "3.9"

services:
  frontend:
    image: node:20
    working_dir: /app
    volumes:
      - ./:/app 
      - /app/node_modules  
    ports:
      - "5173:5173"
    env_file:
      - .env
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"